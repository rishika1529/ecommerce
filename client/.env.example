# Client Environment Variables
# Copy this file to .env and fill in the values

VITE_API_BASE=/api                     # API base URL (use /api for proxy, or full URL for production)
                                       # Examples:
                                       #   /api                         → uses Vite dev proxy
                                       #   https://api.shopverse.com    → direct backend URL
